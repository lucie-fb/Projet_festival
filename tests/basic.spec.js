import { describe, it, expect } from 'vitest';

describe('Vérification du fonctionnement de la suite de tests', () => {
  
  it('devrait passer avec succès pour vérifier que Vitest fonctionne', () => {
    expect(1 + 1).toBe(2);
  });

});
